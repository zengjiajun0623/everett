# build tree: core-geth clone, devnet datadirs, node logs (recreated by scripts)
build
*.log
.DS_Store
dist/*.tar.gz
dist/*-node-*/

# build outputs never belong in the repo (docker/README: "no prebuilt binaries")
stratum/stratum
kawpow-stratum
*.o
